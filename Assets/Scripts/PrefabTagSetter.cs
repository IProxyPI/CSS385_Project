using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PrefabTagSetter : MonoBehaviour
{
    Player_Manager pm;
    
    // Start is called before the first frame update
    void Start()
    {
        // pm = 
        // if this came from opponent
        // possibly based on direction faced?
        // label tag appropriately
    }

    // Update is called once per frame
    void Update()
    {
        
    }


}
